#include <iostream>
#include <math.h>
int main() {
    float n = 2, m = 2, resultado = 0;
    resultado = sqrt(n);
    std::cout << "La raíz " << m << " de " << n << " es: " << resultado << std::endl;
    return 0;
}